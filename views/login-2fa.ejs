<!DOCTYPE html>
<html>
<head>
    <title>二次验证 - <%= settings.site_name %></title>
    <link rel="preconnect" href="https://fonts.loli.net">
    <link rel="preconnect" href="https://gstatic.loli.net" crossorigin>
    <link href="https://fonts.loli.net/css2?family=Noto+Sans+SC:wght@400;500;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/admin.css">
    <style>
        body { display: flex; justify-content: center; align-items: center; height: 100vh; margin: 0; }
        .verification-input { letter-spacing: 5px; font-size: 1.5rem; text-align: center; font-family: monospace; }
    </style>
</head>
<body>
    <div class="login-container">
        <h2>两步验证</h2>
        <p style="color: #666; margin-bottom: 20px;">请输入验证器 App 中的 6 位数字</p>
        
        <% if (error) { %>
            <div style="color: #dc3545; background: #f8d7da; padding: 10px; border-radius: 4px; margin-bottom: 20px;"><%= error %></div>
        <% } %>
        
        <form action="/login" method="POST">
            <input type="hidden" name="password" value="<%= password %>">
            <div class="form-group">
                <input type="text" name="token" class="verification-input" placeholder="000000" maxlength="6" pattern="\d{6}" required autofocus autocomplete="off">
            </div>
            <button type="submit" class="btn btn-primary">验证</button>
        </form>
    </div>
</body>
</html>